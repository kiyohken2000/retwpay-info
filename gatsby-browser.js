/**
 * Implement Gatsby's Browser APIs in this file.
 *
 * antd v4 styles were previously injected per-component by gatsby-plugin-antd
 * (babel-plugin-import). That plugin doesn't support Gatsby 5, so load the
 * full stylesheet globally instead.
 */
import 'antd/dist/antd.css';
